#include <vector>

vector<자료형> v;

v.size();           벡터의 크기
v.resize(숫자);     숫자만큼 크기를 변경
v.capacity();       벡터에 할당된 공간
v.reserve(숫자);    숫자만큼 벡터에 할당
v.push_back(값);    벡터의 마지막 인덱스에 값 추가
v.pop_back();       벡터의 마지막 인덱스 값 제거